# Time backend is allowed to take before socket to client is broken
# GUNICORN_TIMEOUT_VALUE=90

# backend entrypoint - do not edit
FLASK_APP=app.py

# backend verbosity
# CONFLATION_VERBOSITY=DEBUG

# database connection settings
POSTGRES_DB=gis
POSTGRES_HOST=10.5.0.10
POSTGRES_PASSWORD=batimap
POSTGRES_PORT=5432
POSTGRES_USER=docker

# should data be reimported at next start
# FORCE_IMPORT=true
# region to import. By default the whole country (France)
# REGIONS=france/rhone-alpes-latest
# max zoom to cache when application is starting
INITIAL_MAX_ZOOM=7
