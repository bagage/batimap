# Time backend is allowed to take before socket to client is broken
# GUNICORN_TIMEOUT_VALUE=90

# backend entrypoint - do not edit
FLASK_APP=app.py

# backend verbosity
# CONFLATION_VERBOSITY=DEBUG

# database connection settings
POSTGRES_DB=gis
POSTGRES_HOST=10.8.0.10
POSTGRES_PASSWORD=batimap
POSTGRES_PORT=5432
POSTGRES_USER=docker

FORCE_IMPORT=true # should data be reimported at next container start
REGIONS=france/limousin-latest # region to import. By default the whole country (France)

INITIAL_MAX_ZOOM=4 # max zoom to cache when application is (re)starting
